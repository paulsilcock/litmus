# TDD Workflow

TDD applies by default when a change adds, fixes, or modifies behaviour.

Process:

1. Write the smallest failing test for one behaviour.
2. Implement the minimal change needed to pass.
3. Refactor while keeping tests green.
4. Repeat one behaviour at a time.

Use autonomous mode unless the user explicitly asks for guided/collaborative mode.
