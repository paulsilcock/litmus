# Canonical Agent Rules

Read and follow these rule documents:

- `tdd-workflow.md`
- `code-quality.md`

Default workflow policy:

- For any behaviour change, use **autonomous TDD** by default.
- Write one failing test at a time.
- Follow `RED -> GREEN -> REFACTOR`.
- Ask clarifying questions only when expected behaviour is ambiguous or requires product/API decisions.
