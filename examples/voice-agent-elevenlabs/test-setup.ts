// Vitest setup file. Vite-plus config at the repo root references
// `./test-setup.ts` which resolves per-workspace; an empty file here
// satisfies the resolution without imposing any package-specific setup.
